#pragma once

#include "ysJobPool.h"
#include "ysJobQueue.h"
#include <thread>

struct ysWorkerManager;


struct ysWorker
{
    enum
    {

    };

    void Reset();

    void Submit(ysJob* job);
    void Wait(ysWorkerManager*, ysJob* job);

    ysJob* GetJob(ysWorkerManager*);

    ysJobQueue m_jobQueue;
};